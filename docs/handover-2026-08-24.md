> **STALE — superseded 2026-08-28.**  This file describes the step-8
> measurement era and is kept for history only.  The CURRENT handover
> is `docs/handover-2026-08-28.md`.  Do not resume work from this file.

# Handover — the road to the first honest 7595 run (as of 2026-08-24 ~07:00)

State file for resuming the step-8 measurement work.  Read alongside
`docs/de-cheat-execution-plan.md` (steps ladder) and
`docs/honesty-audit-2026-08-18.md` (findings 1-81).

## Where we are

Steps 1-7 are DONE and pushed.  The pre-7595 full-codebase de-cheating
audit (three adversarial sweeps: pipeline, recent diffs, whole runtime)
is DONE and pushed; findings 70-81 all fixed or disclosed.  Ray's
instruction: zero cheating in the measured pipeline; the audit was his
gate for step 8, and it has been satisfied.

Step 8 — the frozen 7595 measurement — is IN PROGRESS.  Five run
attempts so far, each aborted by the coverage check (which refuses
partial credit when a file yields no emaxx outcomes), each abort
diagnosed to a real bug, fixed, gated, and pushed:

1. Attempt 1 (13:23, aborted file 31): emaxx crashed on
   semantic-utest-ia.el -- infinite native recursion in `equal' over
   cyclic overlay graphs.  FIXED in 2a3ac21: overlays_equal threads the
   seen-set (GNU's depth>10 memo semantics); plus a stack-headroom
   guard in eval() that signals excessive-lisp-nesting instead of
   dying (macOS pthread stack introspection, checks every 64 levels).
2. Attempts 2-4 (22:03-23:04, off Ray's tty merge 155fab1): custom-tests.el
   died by signal / timed out.  Root cause was NOT the tests: the
   harness sets EMAXX_TRACE_LOAD_ERRORS=1, and the VM's error tracer
   (a) logged `throw's (seq--break fires constantly as control flow)
   and (b) rendered payloads with derived Debug -- unbounded,
   cycle-blind -- producing 1.2 GB of trace then a stack overflow on a
   cyclic Custom widget graph.  FIXED in 07486bd: tracer skips Throw,
   renders through `bounded_error_debug' (types.rs, ~2 KB cap), and the
   harness now preserves a signal-killed child's stderr in the
   synthesized report (that change is what made this diagnosable).
3. Overnight shakedown (00:36-04:43): full manifest in `run' mode
   (records failures, continues).  494 files: 438 PASS, 56 FAIL (all
   honest mismatch content), ZERO crashes.  Died at the very end when
   macOS's temp cleaner reaped /var/folders mid-copy -- hence frozen
   attempts now run with TMPDIR pointed into the session scratchpad.
   Mining the artifacts exposed 8 files with zero emaxx coverage.
4. Attempt 5 (04:43, aborted at cl-lib-tests.el): led to THE core find
   of the night -- emaxx compared floats in eq/eql/equal with IEEE ==
   where GNU compares REPRESENTATIONS (fns.c same_float).  So
   (eq NaN NaN) was nil, and macroexp-macroexpand's fixpoint loop
   `(while (not (eq form (macroexpand-1 form))))' spun forever on ANY
   file containing a NaN literal.  That one bug explains 6 of the 8
   zero-coverage files: cl-lib-tests, data-tests, fns-tests,
   floatfns-tests, esh-util-tests, dbus-tests.  FIXED (uncommitted,
   in gate v23 now): four float arms switched to to_bits() equality --
   values_eq_in_env (eq), values_eql, values_equal_recursive in
   types.rs AND primitives/values.rs, values_equal_including_properties,
   values_eq_for_substitution (completion.rs).  Also fixes GNU's
   (eql 0.0 -0.0) => nil and (equal 0.0 -0.0) => nil contracts.
   Verified: all six files now load and complete; oracle-parity probe
   (eql/equal/memql on NaN and signed zeros) byte-identical.
   Known residual approximation TO DISCLOSE in the audit doc when
   committing: (eq 0.0e+NaN 0.0e+NaN) on two separately-boxed GNU
   floats is nil there, t here -- immediate floats cannot model boxed
   identity; same pre-existing class as (eq 1.5 1.5).

## In flight right now

- Gate v23 (launched 06:51, ~1 h) over the float-representation fix.
  Log: $SCRATCH/gate-v23.log.  The uncommitted diff is exactly:
  src/lisp/types.rs, src/lisp/primitives/values.rs,
  src/lisp/primitives/completion.rs, src/lisp/primitives/loading.rs
  (a temporary EMAXX_DEBUG_EAGER_MACROEXPAND eprintln probe -- REMOVE
  or keep-and-mention before committing).
- Long-cap probes for the last two zero-coverage files, results land in
  $SCRATCH/py-long.exit and th-long.exit:
  - python-tests.el (cap 3600 s): confirmed actively executing
    interpreted Lisp, believed slow-not-hung (6000-line file, 20-50x
    interpreter ratio).
  - thread-tests.el (cap 1500 s): same signature, could still be a
    cooperative-threading livelock -- if its probe times out, sample it
    (`sample PID 3`) and look for a thread-yield busy-wait.

$SCRATCH = /private/tmp/claude-501/-Users-nbmhqa186-projects-emaxx/1d37c95d-cf33-468c-b97a-33b26419f288/scratchpad

## The resume chain (Ray's standing "go" covers all of it)

1. Wait for gate v23.  If red: fix, re-gate.  If the only failure is
   `upstream_eshell_script_regressions_stay_green' /
   em-script-test/source-script/background, that is the known
   nondeterministic background-subprocess flake (documented in
   07486bd's message; goes pass/fail/pass on identical code) -- rerun
   it serially to confirm, then treat the gate as green.
2. Remove (or bless) the loading.rs debug probe; update the audit doc:
   finding for the float-representation bug + the boxed-NaN-eq
   disclosure; commit as Ray (no AI attribution), push.
3. Check py-long/th-long results.  Both complete => frozen with
   --timeout-seconds 3600.  thread-tests hung => diagnose first
   (likely fix class: thread-yield/condition-wait not advancing the
   scheduler).  python-tests slower than 3600 => raise the cap; the
   timeout only rescues files, it cannot inflate the score.
4. Launch: TMPDIR=$SCRATCH/frozen-tmp cargo run --bin compat-harness --
   frozen --timeout-seconds 3600 > $SCRATCH/frozen-7595-v6.log 2>&1
   (nohup, background; ~3-4 h wall).
5. Report X/7883 with the provenance block from the summary.json in
   target/compat/frozen-*/  (mode frozen, manifest sha, subject/oracle
   sha256s, dirty=false, timeout recorded).  The mode label and artifact
   directory lost their "-7595" suffix on 2026-08-26 when the denominator
   moved to 7,883; a `frozen-7595-*' glob now matches only stale runs.
6. After the number: mine per-file artifacts, cluster mismatches by
   root cause, fix in oracle-probed batches, re-run frozen per batch
   (~the plan for climbing toward 7595).

## Hard-won operational knowledge

- Frozen mode now REFUSES --subject-root and a dirty tree (audit F5/F6)
  -- commit before running; .claude/ is gitignored for this reason.
- Every gate is ~60 min serial; per-file emaxx image setup in the
  harness is ~11-17 s (the in-process test template does NOT apply to
  spawned binaries).
- Probe pitfall that cost an hour twice: `cmd | tail -N; echo $?`
  reports TAIL's exit code.  Redirect to a file and check $? directly.
- Probe timeouts must clear the ~10 s CLI startup; 5 s probes produce
  phantom "hangs" (that false alarm masked the real NaN hang earlier).
- macOS reaps /var/folders temp files during long runs; keep harness
  TMPDIR inside the scratchpad.
- sample(1) needs the emaxx pid, not the timeout wrapper's.
- Flake watch: em-script-test/source-script/background (eshell bg
  output race), subprocess_exit_is_event_driven_... (her earlier
  report), and one 20-min compat_runtime lib-test hang at
  save_buffer_skips_unmodified (2026-08-23, one occurrence, not
  reproduced).

## Commit log of this arc (all pushed, all as Ray)

- 2adbbd4 Step 6, 9dc812f Step 7, 44efaf4 pre-7595 audit close-out,
  9d9ead7 gitignore, 155fab1 Ray's tty merge (theirs), 2a3ac21
  overlay-equal + stack guard, 07486bd tracer fix.
