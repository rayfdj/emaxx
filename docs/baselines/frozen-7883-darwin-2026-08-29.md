# Frozen compatibility baseline, Darwin — 2026-08-29

First baseline recorded under the per-platform contract regime: this
score is a DARWIN statement, measured against the pinned Darwin NS
oracle, and is never compared against the Linux number (which has its
own document and its own oracle).

## Headline

    7,633 / 7,883 outcomes matching  (96.83%)
    250 mismatching, across 42 of 461 files with selected tests
    419 files fully matching

## Movement against the 2026-08-27 baseline (7,620 / 263)

Net +13 matching outcomes, from the obarray batch (finding 112/121),
the tty merge fixups (%z mnemonics, copy-file KEEP-TIME, the gpg
signature unpin) and their siblings.  Computed from the two runs'
artifacts, not from logs:

  - Five files went from mismatching to fully matching:
    package-tests, autoconf-tests, subword-tests, coding-tests, and
    regex-emacs-tests — the last closing the single
    `regex-tests-word-character-class' outcome the 08-27 document
    tracked as an undiagnosed regression.
  - One file regressed: test/src/keymap-tests.el, the single test
    `test-non-key-events'.  Diagnosed as ledger finding 133 (where-is
    full-list ordering, introduced by tty commit ae8f93b) and fixed in
    the batch AFTER the measured tree; the fix is oracle-verified on
    Linux and expected to flip this outcome green on both platforms at
    the next measurement.
  - `semantic-utest-ia-texi' drew suspicion mid-run but the old
    artifacts show the identical failure pre-merge: pre-existing, on
    the fix queue, not a regression.

## Largest remaining clusters (mismatching outcomes per file)

      96  test/src/comp-tests.el          (native comp on a runtime without one)
      29  test/lisp/progmodes/eglot-tests.el
       9  test/src/process-tests.el       (was 14 in the 08-27 baseline)
       9  test/lisp/progmodes/elisp-mode-tests.el
       9  test/lisp/net/tramp-tests.el
       9  test/lisp/erc/erc-tests.el
       9  test/lisp/emacs-lisp/bytecomp-tests.el
       7  test/lisp/simple-tests.el
       6  test/src/emacs-tests.el
       6  test/lisp/net/socks-tests.el
       6  test/lisp/emacs-lisp/gv-tests.el
       4  test/lisp/emacs-lisp/edebug-tests.el
       3  test/src/thread-tests.el
       3  test/lisp/erc/erc-scenarios-internal.el
       3  test/lisp/emacs-lisp/shortdoc-tests.el

The full 42-file mismatch list and the 250 per-test detail lines are
in the run's artifacts (per-file comparison.json is the ground truth,
per the 08-27 correction note).

## Provenance (from summary.json's provenance block, recorded verbatim)

    subject_git_head    44a919b936135988dadff45f6f467367f62f7cad
    subject_git_dirty   false
    subject_sha256      225039fd8a94d22939566cd37140ae9f0b6eaddd7b3d2b853411c270f6b58002
    subject_profile     gate
    oracle_repo_commit  636f166cfc86aa90d63f592fd99f3fdd9ef95ebd
    oracle_sha256       7d8944fe2b2bdbd2856cfd4f47dbd5c80db90089ac20be641c10a348bf217e82
    oracle_emacs_version 30.2
    oracle_system_type  darwin
    timeout_seconds     3600

The measured tree is 44a919b: the per-platform-contracts commit
(b4851e0) landed after this run began and touches only harness-side
manifest selection, so the score stands for the runtime it measured.
The finding-117 eshell/em-script load-noise caveat from the 08-27
document still applies (up to ~2 outcomes of run-to-run noise).

Artifacts: target/compat/frozen-1788002917928890000-3524/ on the
Darwin machine that ran the measurement; the full summary.json
snapshot lives there (not committed from this run — the measuring
machine can snapshot it beside this file in a later commit).
