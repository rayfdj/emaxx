# Frozen compatibility baseline — 2026-08-27

## Headline

    7,620 / 7,883 outcomes matching  (96.66%)
    263 mismatching, across 46 of 461 files with selected tests
    415 files fully matching

First measurement against the 7,883-outcome denominator (finding 98's
regeneration), and the first to include eglot-tests.el, tramp-tests.el and
test/src/comp-tests.el — the native-compilation suite had never been measured
before this run.

## Read the percentage with its history in mind

The previous baseline was 7,423 / 7,595 (97.74%).  The headline FELL while the
project improved, and both halves are real:

  - On the old 7,595-outcome subset, this tree scores 7,466 matching
    (98.30%): mismatches fell 172 -> 129, a net gain of 43 outcomes from the
    honest fixes landed between the two runs.  Nine previously mismatching
    files now match: color-tests, macroexp-tests, multisession-tests,
    erc-nicks-tests, em-script-tests, python-tests, ruby-mode-tests,
    css-mode-tests, wid-edit-tests.  json-tests (both) and map-tests — the
    finding-123 load-path casualties — are among the passing files.
  - The 288 newly measured outcomes contribute 134 mismatches (154 match).
    Roughly 109 are comp-tests.el: native-compilation behaviour on a runtime
    with no native compiler.  Honest, expected, and now visible instead of
    excluded.

One file REGRESSED against the old baseline: test/src/regex-emacs-tests.el,
ONE mismatching outcome — the single test `regex-tests-word-character-class'.
Tracked; not yet diagnosed.

## Largest remaining clusters (mismatching outcomes per file)

      96  test/src/comp-tests.el          (native comp; first measurement)
      29  test/lisp/progmodes/eglot-tests.el   (first measurement)
      14  test/src/process-tests.el
       9  test/lisp/progmodes/elisp-mode-tests.el
       9  test/lisp/net/tramp-tests.el         (first measurement)
       9  test/lisp/erc/erc-tests.el
       9  test/lisp/emacs-lisp/bytecomp-tests.el
       7  test/lisp/simple-tests.el
       6  test/src/emacs-tests.el
       6  test/lisp/net/socks-tests.el
       6  test/lisp/emacs-lisp/gv-tests.el

All except the first-measurement files were already mismatching in the
2026-08-25 baseline; the only regression is the single regex outcome above.

CORRECTION (same day): the first push of this document said the regex
regression was 7 outcomes and listed a cluster table with coding-tests at 19,
print-tests at 16 and several 12-14s.  Those numbers came from grepping the
run LOG with a fixed-width context window, which miscounts by picking up
neighbouring files' lines — the same trap that briefly inflated comp-tests to
"~157 matching" during the run.  This table is recomputed from the per-file
comparison.json artifacts, which are the ground truth.  The subset arithmetic
(129 / 134 / 98.30%) was always computed from the artifacts and is unchanged.

## Provenance (from summary.json, recorded verbatim)

    subject_git_head    bbe441facd75de612f613af3a2fd5d3d18657ff9
    subject_git_dirty   false
    subject_sha256      f75d609f56a82c505e270fac3032d30ab9c910dbe6e717b740782da08ae510db
    oracle_repo_commit  636f166cfc86aa90d63f592fd99f3fdd9ef95ebd
    oracle_sha256       7d8944fe2b2bdbd2856cfd4f47dbd5c80db90089ac20be641c10a348bf217e82
    timeout_seconds     3600
    mode                frozen   (renamed from frozen-7595; the label no
                                  longer embeds a denominator)

Caveat inherited from finding 117: the eshell background-source race fails
50-75% of runs under load.  em-script-tests.el matched in THIS run; a rerun
could flip it, moving the headline by up to 2 outcomes.  That noise is a known
defect, not measurement error, and is tracked.

Artifacts: target/compat/frozen-1787808391866311000-33238/
